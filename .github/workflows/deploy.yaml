name: Deploy Frontend
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: docker login -u ${{ env.DOCKER_USERNAME }} -p ${{ env.DOCKER_PASSWORD }}
      - run: docker build -t arvinbordios/react-test -f Dockerfile.dev .
      - run: docker run -e CI=true arvinbordios/react-test npm test
